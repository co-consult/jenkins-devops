pipeline {
    agent any
    triggers {
        pollSCM('0 8 * * *')
    }
     environment {
        IMAGE_NAME = 'cogeex:eureka-latest'
        DEPLOYMENT_FILE = 'cogeex-eureka.yaml'
    }
        
    stages {
        stage('Checkout Repo') {
            steps {
                script {
                    checkout([$class: 'GitSCM',
                        branches: [[name: '*/develop']],
                        extensions: [],
                        userRemoteConfigs: [[
                            credentialsId: 'azure_secret', 
                            url: 'https://dev.azure.com/CoConsult/CoGeex%20v2/_git/cogeex-eureka'
                        ]]
                    ])
                }
            }
        }
        stage('Build Docker Image') {
            steps {
                script {
                    docker.build("${IMAGE_NAME}", "-f Dockerfile .")
                }
            }
        }
        stage('Delete old ressources') {
            steps {
                script {
                    checkout([$class: 'GitSCM',
                        branches: [[name: '*/DEVOPS-13']],
                        extensions: [],
                        userRemoteConfigs: [[
                            credentialsId: 'git_secret', 
                            url: 'https://github.com/co-consult/jenkins-devops'
                        ]]
                    ])
                    
                    sh '''
                if kubectl get -f cogeex-eureka.yaml > /dev/null 2>&1; then
                    echo "Deleting existing Kubernetes resources..."
                    kubectl delete -f cogeex-eureka.yaml
                else
                    echo "No existing resources to delete."
                fi
                if kubectl get -f cogeex-gateway.yaml > /dev/null 2>&1; then
                    echo "Deleting existing Kubernetes resources..."
                    kubectl delete -f cogeex-gateway.yaml
                else
                    echo "No existing resources to delete."
                fi
                if kubectl get -f cogeex-auth-service.yaml > /dev/null 2>&1; then
                    echo "Deleting existing Kubernetes resources..."
                    kubectl delete -f cogeex-auth-service.yaml
                else
                    echo "No existing resources to delete."
                fi
                if kubectl get -f cogeex-company-service.yaml > /dev/null 2>&1; then
                    echo "Deleting existing Kubernetes resources..."
                    kubectl delete -f cogeex-company-service.yaml
                else
                    echo "No existing resources to delete."
                fi
                if kubectl get -f cogeex-profile-service.yaml > /dev/null 2>&1; then
                    echo "Deleting existing Kubernetes resources..."
                    kubectl delete -f cogeex-profile-service.yaml
                else
                    echo "No existing resources to delete."
                fi
                if kubectl get -f cogeex-project-service.yaml > /dev/null 2>&1; then
                    echo "Deleting existing Kubernetes resources..."
                    kubectl delete -f cogeex-project-service.yaml
                else
                    echo "No existing resources to delete."
                fi
                if kubectl get -f cogeex-parser-service.yaml > /dev/null 2>&1; then
                    echo "Deleting existing Kubernetes resources..."
                    kubectl delete -f cogeex-parser-service.yaml
                else
                    echo "No existing resources to delete."
                fi
                if kubectl get -f cogeex-matching-service.yaml > /dev/null 2>&1; then
                    echo "Deleting existing Kubernetes resources..."
                    kubectl delete -f cogeex-matching-service.yaml
                else
                    echo "No existing resources to delete."
                fi
                 '''
                }
            }
        }

    }
}